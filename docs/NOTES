Planets can't follow the same rules as ships with gravity. They will need to be
"on rails". When a ship leaves a planet have it take on it's parent planet's
velocity at that time.

rotation 0 is facing right

"non-straight" lines in hex coords have two equally valid paths, make pathfinder check and choose from both when avoiding collisions

should I do a like NCIS "enhance" sort of thing for zooming? Like you select a rectangle then it stutters forwards

takeoff - have larger planets cost more fuel to take off

UI console, to report events/numbers to make game rules more transparent without
having to make a good UI